# Paper Folder Release Audit

Audit scope: `paper/`.

Status: passed for the current working tree.

- All added paper materials are scoped under `paper/`.
- Example transcripts use synthetic participant IDs `ex001` through `ex006`.
- No generated Python bytecode directories are present under `paper/`.
- No macOS metadata files are present under `paper/`.
- Expected outputs are generated from the sanitized examples only.
- Restricted study data and full paper feature matrices are represented by
  checksums only; the underlying restricted data files are not redistributed.
